let player; 

function Player (classType, hull, firepower, accuracy){
    this.classType = classType;
    this.hull = hull;
    this.firepower = firepower;
    this. accuracy = accuracy;
}

let PlayerMoves = {
    calcAttack: function(){
        // Who attacks first?
        let getPlayerAccuracy = player.accuracy;
        let getEnemyAccuracy = enemy.accuracy;

    }
    let playerAttack = function(){
          
    }
}
